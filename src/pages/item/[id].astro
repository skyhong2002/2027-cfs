---
import Experience from "../../components/cfs/Experience.astro";
import { getAvailableItemIds } from "../../utils/items-loader";
export const getStaticPaths = async () => (await getAvailableItemIds()).filter(id => id !== "18").map(id => ({ params: { id } }));
---

<Experience initialItem={Astro.params.id} />
