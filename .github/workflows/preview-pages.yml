name: Deploy preview to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: preview-pages
  cancel-in-progress: false

jobs:
  build:
    if: github.ref == 'refs/heads/main' && github.event.repository.fork
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Configure Pages
        id: pages
        uses: actions/configure-pages@v5

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Validate sponsorship data
        run: pnpm validate-data

      - name: Test catalog validation
        run: pnpm test:data

      - name: Test proposal calculations
        run: pnpm test:proposal

      - name: Check Astro project
        run: pnpm test

      - name: Build preview with committed data
        run: pnpm build
        env:
          SITE_URL: ${{ steps.pages.outputs.origin }}
          BASE_PATH: ${{ steps.pages.outputs.base_path }}
          CFS_PREVIEW: "1"

      - name: Install browser for smoke tests
        run: pnpm exec playwright install --with-deps chromium

      - name: Test complete preview flows
        run: |
          pnpm preview --host 127.0.0.1 --port 4321 > /tmp/cfs-preview.log 2>&1 &
          CFS_TEST_URL="http://127.0.0.1:4321${BASE_PATH}/" pnpm test:e2e
        env:
          BASE_PATH: ${{ steps.pages.outputs.base_path }}

      - name: Upload site
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
